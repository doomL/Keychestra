[project]
name = "keychestra"
version = "0.2.0"
description = "Type, work, and jam over your music — keyboard SoundFont overlay for Linux"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.scripts]
keychestra = "keychestra.__main__:main"
