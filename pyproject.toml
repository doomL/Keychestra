[project]
name = "organ-bg-worker"
version = "0.2.0"
description = "Background keyboard organ — type normally, play SoundFont instruments"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.scripts]
organ-bg = "organ_bg.__main__:main"
